name: 'Test'

runs:
  using: 'composite'
  steps:
    - name: Setup
      uses: ./.github/actions/setup

    - name: Run headless test
      uses: coactions/setup-xvfb@v1

    - run: npm run build
      shell: bash

    - run: npm run test
      shell: bash
